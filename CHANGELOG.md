# Changelog

本项目版本号遵循 `0.x` 阶段的语义化：`0.<minor>.<patch>`；预发布版本带 `-beta.N` 后缀（面板中显示为 `0.1.1beta1`）。

> 本仓库是 [WestFox-AwA/dsh-prompt-optimizer](https://github.com/WestFox-AwA/dsh-prompt-optimizer) 的 **fork**。
> **功能层与上游 v0.1.9-beta.1 对齐**（上下文双模式、难度→开发长度编排、提示词硬化、auto-send 修复等，见下方各版本小节）；
> **界面层是本 fork 自己的**。下面从 v0.1.9-beta.1 起是上游的发布历史。

## v0.3.0-beta.1 — 本 fork（新增：提示词收藏夹）

### 提示词收藏夹（新功能）

- **三处入口**：胶囊主菜单里加「提示词收藏夹」；拦截卡**动作栏**里加「收藏本条」与「收藏夹」（挨在一起，
  一个存一个看）；**对话里每条你发出的消息**下面、复制按钮旁边也有一枚书签按钮。
- **对话里那枚按钮是 DOM 注入**：官方只给助手消息那排动作留了插槽（`conversation.chat.assistant-actions`），
  用户消息那排是 `UserMessageNodeView` 里硬编码渲染的、没有槽可挂。做法是识别「含复制按钮、且时钟在左
  （`timeStart`）」的动作行（实测只有用户消息是这种形态），把按钮插到复制按钮之后，
  **class 直接复用复制按钮的**，所以样式 / 尺寸 / hover 完全一致。插进去的节点在 React 之外，
  因此带**自愈**：每 2.5 秒扫一遍，被重渲染清掉就补回来。
- **收藏的是优化后的产出**：点「收藏本条」存的就是此刻卡片里的产出（**含你手动编辑过的版本**）；
  正文完全相同时自动去重，不会存两条。
- **面板是一个居中的对话框**（右上角 × / Esc / 点遮罩关闭），不是挂在按钮旁的小弹层：
  收藏夹要放列表 + 搜索 + 管理，本身就不是"一条小提示"；而它的入口有两个、分别在屏幕上下两头 ——
  挂其中一边，从另一边进来就会显得跑到别处去了，居中对两边一致。
- **面板内容**：列出 + 搜索（按标题或正文过滤）。
- **点一条 → 填入输入框，不自动发送**。等价于你重新打了一遍草稿，所以它会再走一次优化 ——
  想原样发出请用卡片上的「放行原文」。
- **编辑**：标题与**正文**都能改（正文是多行框，Ctrl/Cmd+Enter 保存、Esc 取消；标题留空自动取正文首行）。
- **管理**：置顶（常用靠前，且不会被上限淘汰）/ 删除（二次确认）/ 清空全部（二次确认）。
- **导入 / 导出**：导出为 JSON 文件（可备份或分享）；导入按「id + 正文」去重，只增不改。
- **存储**：全局共享、**独立文件** `~/.dsh/prompt-optimizer-favorites.json`，最多 200 条
  （超出丢最早的，但**不丢置顶的**）。全部读写都在本机，不上传任何地方。

### 实现要点

- 宿主侧新增 `GET / POST /prompt-optimizer/api/favorites`：GET 读列表，POST 带 `action`
  做 `add` / `update`（改标题与正文、置顶）/ `remove` / `clear` / `import`。
  `update` 里**先写正文再定标题**：标题留空时的兜底要按新正文取首行，否则会拿旧正文算。
- 所有写操作都由**宿主回传完整列表**、客户端整体替换 —— 不做本地乐观更新，避免两边不一致。
- 收藏条目：`{ id, title, text, tier, source, pinned, createdAt, updatedAt }`；标题缺省取正文首行。

## v0.2.0-beta.1 — 本 fork（基于上游 v0.1.9-beta.1）

### 界面（本 fork 特有）

- **入口合并**：原来散在输入框左侧的档位滑块、权限滑块、模型胶囊、`?` 按钮，合并成输入框底部的一枚
  `✦ 优化 <档位>` 胶囊；点开是纵行菜单：**档位 / 权限 / 模型 / 上下文 / 使用帮助**。
  会话里与新对话页**同一处、行为一致**（新对话页没有会话头部，原来那里根本没有入口）。
- **菜单用官方基元**：`@deepseek-ai/dsh-client-ui-primitives` 的 `Menu` + 扁平描边图标 ——
  外壳、行高、选中 ✓、portal 定位、点击外部关闭、视口夹取全部复用官方实现。
- **模型改为子菜单**：主菜单只保留「会话默认 + 各提供商」，**悬停某家 provider 时从它右侧飞出该家的模型列表**。
  子菜单官方不画 ✓，所以"当前选中的那个"用右侧说明标出；provider 行在收起状态也会写「当前 · 模型名」。
- **上下文改为一整行滑块**：不再占 7 行预设，一行里同时给出「最近 N 回合（0~10）」滑块与「全文」开关。
  它放在 `type:"label"` 的菜单项里 —— label 渲染成 `<div>` 而不是 `<button>`，
  所以在里面拖动 / 点击不会误触发"选中这一项"。**主菜单因此从 19 项缩到 12 项（612px → 438px）**。
- **窄窗口给子菜单留位**：官方子菜单是"从父项向右飞出"且**不做视口夹取**，窄窗口下会飞出右边、点不到
  （实测 700px 宽时右侧溢出 79px）。改用官方 `getAnchorRect` 在窗口不够宽时把整张菜单左移，
  保证飞出的子菜单完整可见 —— 620 / 700 / 780 / 900 / 1000 / 1440 六档实测全部在屏内。
- **拦截浮窗 → 输入框上方的折叠卡**：挂在 `conversation.input.dock`（与官方待办面板、队列面板同槽），
  是布局流里的正常占位，不浮动、不遮挡对话内容。头部一行即折叠开关、审查态自动展开；
  产出是自动增高的可编辑文本框；「思考」「已查证」默认折成一行；
  动作栏固定四个按钮（`‹ 回退` / `放行原文` / `重新生成` / `确认提交`），位置与数量在任何状态下不变。
- **删除自造的一整套窗口管理**：`clampPos` / `clampSize` / `applyGeom` / `reflowOverlay` / `persistUi`、
  拖拽与右下角缩放手柄、位置尺寸落盘、`shell.overlay` 占用。

### 与上游的功能性差异

- **注册幂等**：上游 v0.1.3 用"不同优先级"规避 `list slot ... already has an entry` 报错，
  但该槽的去重键是 `priority` 而不是 `order`；本 fork 把注册改成幂等（重复调用直接返回）。
- **探针闸门**（对齐上游 v0.1.4 的三道闸门）：未知 `cmd.run` 一律拒绝并记 `cmd-unknown`，
  **绝不回落到通用探针**；探针必须显式带 `deliver: true` 才允许投递。
- **`history` 事件的竞态**：上游 `publishRun` 只写给当前订阅者、没有回放缓冲，而 `history`
  是在开跑最开始发的 —— 客户端要等 `/run` 返回后才建 EventSource，多半已经错过，
  于是"本次读入多少上下文"时有时无。本 fork 让 SSE 的**初始快照也带上 `history`**，早订晚订都能拿到。
- **帮助面板首次打开定位错误**：面板尚未进入 DOM 就求位置（量得高度为 0），会从错误坐标向下铺开、
  跑出屏幕；改为先隐藏渲染、量到再显示，并按"胶囊上方剩余空间"限制高度。

## v0.1.9-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**修复：放行/回退后仍被自动发出第二条（表现为「排队发送」）**
- 根因：`releaseOriginal()` / `rollbackYes()` 只清了 `store.run`，但 SSE 事件闭包里持有的那份 run 对象依旧存活；`done` 事件只看 `store.permission === "auto"` 就调用 `autoSend()` → 用户已经放行或回退之后，又自动提交了一次（第二次撞上正在处理的会话，于是变成「排队发送」）。回退路径更严重：面板明确承诺「不发送任何消息」。
- 修法：给运行加"已被用户终结"标记（`run.settled = released / rolled-back`），`autoSend()` 与 `done` 分支都先看这个标记；`autoSend` 自身置 `settled = auto-sent` 保证同一次运行只自动发送一次；放行时同时关流并向后端 `/run/abort`（不再白烧 token）。
- 回归测试 `release-race-demo`（客户端自检，提交用计数桩、不真发消息）：放行后 `submit` 增量必须是 1、回退必须是 0、以及"流仍活着且 `done` 真的到达"时必须是 0（该场景打出 `done-after-settle` 证据）。
- 负对照（保留同一份自检、只拆掉守卫）实测 `PASS=false`：放行用例 `submitDelta=2`、`sentFlag=true`，第二次发出的正是优化后的文本；守卫用例漏发 1 次 —— 证明该缺陷真实存在且此测试抓得住它。
- 修复版复验：`PASS=true`，`submitDelta` 分别 1 / 0 / 0，客户端报错 0 行。

**同批一并发布：传话 AI 提示词的提效与硬化（详见 PROMPT-OPTIMIZATION.md）**
- 三档 system 改为共用部件 + `buildSystem(tier, { historyMode })` 组装；重复表述三处合一，普通档 1445 → 1319 字符（−8.7%）。
- 约束力硬化：长度上限、逐句回溯删除、软措辞禁令（尽量/最好/建议… → 必须/不得/判据）、硬约束三分类 + 每条违反时的处置 + 不得替用户承诺、事实纪律白名单化。
- 新增**难度 → 开发长度**判定：轻（默认，禁止建 goal/todo/计划）/ 中（3~6 条 todo）/ 重（建 goal + 分阶段 + 每阶段验证）；高危信号命中即不得停留在轻档；未命中不得升档、不得因字数多升档。带标准答案的判定用例终测 12/12，漏隐患 0、徒增开销 0。
- 修复全文模式下的提示词谎报（旧文案称"工作 AI 回复只留长度"，而全文模式恰恰发送全文）——拆成回合/全文两份纪律，按 `run.historyMode` 注入。

## v0.1.8-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**上下文模式：回合（0~10）/ 全文（与工作 AI 看到的上下文一致）**
- 回合量程从 0~100 收到 **0~10**：100 回合足以把优化模型的上下文撑爆；宿主夹紧同步改为 10（写 999 → 10、写 12 → 10、写 -5 → 0）。
- 滑块右侧**贴着**一枚「回合 / 全文」转化按钮（同一胶囊内、竖线分隔、点一下换一态）：
  - **回合**：滑块 0~10 档，只带用户原话（工作 AI 回复只留长度与工具次数，防污染）。
  - **全文**：滑块两档 **关 / 开**；开启后把工作 AI 当前看到的完整上下文（双方全文）交给优化模型，总预算 **60000 字**，超限**整回合省略**、绝不截断单条约束。
- 实测（活会话 `session-ae0b2c09…`）：回合态 `turns=10 → 10 回合 / 10356 字 / 省略 0`；全文态 → `25 回合 / 57591 字 / 整回合省略 93`；全文态关闭 → 不带历史（`history:null`）。
- 模式与开关随配置落盘（`historyMode` / `fullOn`），非法值自动回落 `turns`（写 `bogus` 读回 `turns`）。

**UI 扁平化极简（对齐 DSH 观感）+ 底栏压缩**
- 去掉渐变、内阴影、玻璃拟态与"实体感"高光：胶囊 / 滑块 / 模型钮 / 帮助钮统一 **26px 高**、**7px 圆角**、单层平色底 + 1px 描边；轨道 3px、圆钮 12px 平色。
- 底栏行高 26px 且贴底：实测宿主容器 28px、控件行 26px、底边对齐 → **不再把输入框挤上去**。
- 圆钮两端各内缩 6px（新增 `.dpo-range-inner` 行程道）：0% / 100% 时圆钮**不再越出轨道**（修复前实测越界 8px）。
- 交互与无障碍不变：拖动 / 点击 / ←→（Shift ±10）/ PageUp-Down / Home-End；`role=slider` 落在轨道上（胶囊去掉 slider 语义，避免按钮嵌进 slider），回合态 aria `max=10`、全文态 `max=1` 且文案「全文 关 / 开」。
- 修复：底栏内的按钮不再被「发送」判定误吞（`[data-dpo="controls"]` 直接放行）。

**自检**
- `range-demo`：两模式共 5 个采样点（回合 0/5/10、全文 关/开），文本、几何（圆钮居中比例与设计值逐点一致）、aria、按钮两态切换、扁平化（无渐变 / 无阴影 / 26px）全部通过 —— `pass:true`。
- 模式按钮经两次合成点击实测 `turns → full → turns`。

## v0.1.7-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**UI 归一化：上下文控件改为「数字滑块」+ 三控件统一尺寸**
- 上下文控件去掉中文标签（不再显示「上下文 12 / 上下文 关」），**滑块上直接显示当前回合数**（等宽数字，避免宽度抖动），一眼可知当前设定。
- 形态改为**固定宽度滑块 + 数字写在滑块上**：原实现"每格一个标签"，100 档时每格仅约 8px，文字必然溢出边框；新实现滑块恒为 40px 宽，数字永远在框内。
- 三枚胶囊统一 **30px 高**、12px 字号、同圆角同配色；模型胶囊与帮助按钮同高 → 整行视觉归一。
- 交互：拖动 / 点击 / ←→（Shift 加速 ±10）/ PageUp-Down / Home-End；`role=slider` + `aria-valuenow/valuetext`。

**默认值与会话回退**
- 读取回合数默认 **0**（= 改动前的等价行为），客户端与宿主默认值同步为 0；随时可调大。
- 历史总预算 **12000 字**：超出时**按整回合丢弃最早的**，并在上下文里标注省略了几个回合——**绝不截断单条约束条款**。
- 实测：`N=0 → 无历史`；`N=10 → 10 回合 / 2855 字 / 省略 0`；`N=100 → 保留 26 回合 / 10082 字 / 整回合省略 74`。
- 配置往返与夹紧实测：写 12 读回 12；写 999 → 100；写 -5 → 0。

## v0.1.6-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**优化 AI 读入最近 N 回合上下文（0~100，可调）+ 防污染设计**

- 新增「上下文」胶囊滑块（与档位/权限同款长圆框 + 内部小滑块）：0 = 不读，档位为非线性刻度 ,1,2,3,4,6,8,12,16,24,32,48,64,80,100，兼顾"近几回合"的精度与 100 回合的量程；格内只显示当前档文字，避免糊成一团。
- 宿主通过 `ctx.sessions.get(id).ownEvents()` 读取真实会话事件：**用户原话全文**进入上下文（单条上限 4000 字）；
  **工作 AI 的回复被刻意只保留长度与工具调用次数，内容一律不喂**——只借"用户想要什么"，绝不吸收工作 AI 的方案/结论/术语/口吻。
- 提示词里新增 `HISTORY_RULES`（三档共用）+ 历史块自带纪律行；历史与用户原话冲突时以原话为准。
- 运行状态行新增「上下文 8 回合 · 873 字」徽标（`run.history`），可直观确认这次究竟读了多少。
- 落盘新增 `turns`（全局偏好，0~100 夹紧）；`/runs` 遥测暴露 `turns` 与 `history`。
- 实测：`turns=8 → history={turns:8, userTurns:8, chars:873, degraded:false}`（`chars` 只累计用户文本，证明工作 AI 内容确实未进入上下文）。
- 隐私说明更新：上下文**在本机读取**，只有用户文本会随优化请求发给优化模型；工作 AI 的回复内容不会离开本机。
## v0.1.5-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**胶囊滑块（更简洁的控件形态）**
- 档位/权限改为**一个长圆框、里面一个小滑块滑动**：取消外侧标签与数值胶囊，控件行更短更干净。
- **单元对齐**：滑块覆盖当前档位整格（left=i/N、宽=1/N），点击/拖动命中用同一套单元坐标 floor(t*N) —— 视觉、命中、键盘三者同源。
- 语义色延续：档位 灰/蓝/紫/橙、权限 绿/琥珀，滑块底色与选中文字同色系；hover 强调色描边 + 外发光。
- 无障碍保留：role=slider / aria-valuenow / aria-valuetext / tabIndex + ←→ Home/End。
- 宽度显式化（内部绝对定位）：档位 152px、权限 98px，并按窗口宽度三档收缩。

## v0.1.4-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**修复：探针标记可能泄漏进真实会话（作者失误的根因修复）**

事故：作者发了一条**未知命名的自检命令**，而当时浏览器里是旧版客户端 → 命令没有匹配到任何自检分支，**回落到通用探针分支**并启动了整套 19 步探针；随后热重载把探针**中途打断**，其收尾自清场没跑完，于是探针用于"真投递验证"的 `DPO-基线测试-<token>` 标记被提交进了真实会话。

三道闸门（都已实测）：
1. **未知命令一律拒绝**：`run` 不等于 `selftest` 时只记 `cmd-unknown` 埋点并返回，**绝不回落探针**。回归实测：`run=slider-demo-TYPO` → 仅 `cmd-unknown`，无 `probe-start`。
2. **探针需显式投递授权**：`selftest` 还必须带 `deliver:true`，否则记 `probe-refused` 并跳过 —— 探针的投递步骤再也不会"顺手"发生。
3. **两侧兜底清扫**：客户端启动 1.2s 后 + 每 60s 调一次 `/inbox/sweep`；宿主开机时对 `DPO-` 做一次权威清扫。实测队列：全部会话 `nextTurn/nextStep` 为空，**零残留**。
## v0.1.3-beta.1 — 2026/09/14

作者：啃轮胎的西狐

**采纳社区 PR 报告的真实缺陷（已修复并实测）**
- **长草稿被截断**（PR#4 @ChinaBuleSky）：`record()` 是遥测行、会把 text 截到 160 字，而拦截路径把它的返回值当成了功能载荷 → 超过 160 字的输入只有前 160 字进入优化器。现改为另取草稿原文；实测 255 字长草稿完整送达（`sent=255, captured=255`）。
- **滑块刻度与拇指坐标不一致**（PR#1 @NekoDD-wow、PR#2 @S-AN-Shu）：刻度点用 flex 中心 `(i+0.5)/n`、拇指/填充用 `i/(n-1)`，四档里有三档会点错。现刻度层与拇指同用 `i/(n-1)`；实测四点偏差全为 0，逐点点击 4/4 命中。
- **滑块可达性**：补 `role=slider` / `aria-valuenow` / `aria-valuetext` / `tabIndex` 与 ←→ Home/End 键盘操作。

**学自社区风格的 UI 升级**
- **语义化颜色**：档位 关闭=灰 / 普通=蓝 / 高级=紫 / 极端=橙；权限 审查=绿 / 自动=琥珀 —— 填充、拇指、刻度、数值标签同色系，一眼看出状态。
- **刻度层重做**：独立层、与拇指同源，灰点 11px/72% + 描边，选中态外扩；仍保留逐档可点与 tooltip。
- **结果悬浮球**（PR#1 思路）：发送成功后把结果收成一颗按档位配色的小球（可拖动、单击展开回看），并新增**已发送只读**态（「已发送 · 仅供查看」，不再出现「确认提交」，`confirmSubmit` / `releaseOriginal` 在已发送态直接短路，杜绝重复发送）。
- 控件行自愈重挂改用不同优先级，消除 `list slot ... already has an entry` 报错。
## v0.1.2-beta.1 — 2026/09/11

作者：啃轮胎的西狐

UI 迭代：更合理、更人性化、更极简、更优雅。

**控件行**
- 两个滑块加**内联名称**（档位 / 权限），首次使用不必靠 tooltip 猜。
- 移除会顶动布局的**行内提示条** → 改为**浮动通知气泡**（固定于输入框上方，不参与行内布局）。
- 移除行内**拦截累计徽标** → 收进标题栏小徽标与帮助面板抬头（信息不丢，行更干净）。
- 滑块与模型胶囊之间加**发丝分隔线**，形成"强度 / 权限 ｜ 模型 ｜ 帮助"的分组；帮助按钮改为无边框幽灵样式。

**迷你窗**
- 标题栏极简：状态点 + 「提示词优化」+ **本次运行档位徽标** + 拦截计数；尺寸提示改为悬浮才显形。
- 删除冗余的「档位/权限/拦截累计」信息行。
- **思考 / 原文 / 查证动作** 三块改为**可折叠**（默认收起，各带一行摘要：token 与字数 / 原文字数 / 查证步数）；运行中思考自动展开，完成后自动收起，把空间让给产出。
- 审查态**不再重复渲染只读产出面板**（可编辑文本框即产出）；错误重试/默认模型重试移入常驻底栏。
- 回退按钮统一到**常驻底栏一处**（审查态底栏为 回退 / 确认提交 / 重新生成 三键）；回退确认改为内联卡片。

**其他**
- 折叠箭头带旋转动效、通知气泡带弹入动效；`prefers-reduced-motion` 下全部关闭。
- 自检扩充：`run-demo` 上报标题栏/折叠状态/底栏按钮/whether-toast，`controls-demo` 上报内联标签与行内冗余元素是否存在。
## v0.1.1-beta.1 — 2026/09/11

作者：啃轮胎的西狐

首个对外分享版本。核心能力：

- **发送接管**：捕获阶段拦截回车与发送按钮（`Shift+Enter`、`/` 命令、空草稿、卡片外回车一律放行）。
- **传话者语义**：优化 AI 明确是"把用户意思转达给工作 AI"的传话器 —— 不回答用户、不替用户干活、不向用户提问；产出是**可直接发送的命令正文**，无「优化后的提示词 / 改动说明」这类元话语。
- **三档强度**：普通（语言精确化，约 3 秒）／高级（补"显然需要"的约束与验收，约 20 秒）／极端（只读查证项目结构 → 分阶段行动计划 + 验收标准 + 多情况预案，约 20 秒）。
- **两种权限**：需要审查（产出可编辑，确认后发送）／自动输出（完成即发送；失败也按原文发出，绝不静默吞消息）。
- **迷你窗**：可拖动、可改尺寸（记忆）、按会话隔离、常驻底部操作栏（窗口再小按钮也不消失）、思考/产出双通道流式 + 流式光标 + 自动跟随滚动、**思考 token 计数**。
- **按会话独立的档位与权限**：A 会话的设置不影响 B。
- **模型独立**：优化模型与对话模型互不影响；目录预热 + 双层缓存 + 单家超时，死模型自动回退。
- **使用帮助**：控件行 `?` 面板（怎么用 / 档位 / 权限 / 迷你窗按钮 + 推荐组合）。
- 测试与证据：`ACCEPTANCE.md` 逐格验收清单；`evidence/` 内为自检报告、三档对照、几何与产物形态回归等机器留痕。

### 已修复的典型缺陷（详见 `ACCEPTANCE.md`）

- 回退 / × 点击无效（pointerdown 的 `preventDefault` 抑制了 click）。
- 审查态看不到确认提交 / 重新生成（浮层内容被裁且不可滚动）。
- 浏览器缩小后弹窗出现在不可见坐标（开窗未夹紧）。
- 开启档位后"无法发送"（优化模型不可用 + 失败被静默吞掉）。
- 弹窗不再显示（React #310：hook 写在 early return 之后）。
- 模型目录加载不出来（客户端从未拉取 `/models`）。
- 右下角拖不动改尺寸（手柄被常驻底栏盖住 + `onSizeUp` 丢掉最后一次位移）。
- 优化 AI 误以为在跟用户对话（角色与用户消息都缺少"传话"框架）。
